@model IEnumerable<courseManagementSystemV1.Models.User>

<!DOCTYPE html>
<html>
<head>
    <title>HR Management</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            flex-direction: column;
        }

        .hr-list {
            width: 80%;
            margin-top: 20px;
        }

            .hr-list table {
                width: 100%;
                border-collapse: collapse;
            }

            .hr-list th, .hr-list td {
                border: 1px solid #ccc;
                padding: 8px;
                text-align: left;
            }

            .hr-list th {
                background-color: #17a2b8;
                color: white;
            }

        .action-button {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
        }

            .action-button:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div class="hr-list">
        <h1>HR Management</h1>
        <table>
            <tr>
                <th>User ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Actions</th>
            </tr>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.UserID</td>
                    <td>@user.UserFirstName</td>
                    <td>@user.UserLastName</td>
                    <td>
                        <button class="action-button" onclick="location.href='@Url.Action("MakeAdmin", "Admin", new { id = user.UserID })'">Make Admin</button>
                        <button class="action-button" onclick="location.href='@Url.Action("DeleteAdmin", "Admin", new { id = user.UserID })'">Delete Admin</button>
                    </td>
                </tr>
            }
        </table>
    </div>
</body>
</html>
